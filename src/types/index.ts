/**
 * Tipos compartidos del sistema (reexportación desde lib para convención types/).
 */
export type {
  ApiResponse,
  ApiError,
  PaginationMeta,
  TokenPayload,
  TokenPair,
  UserSession,
} from '@/lib/tipos';
